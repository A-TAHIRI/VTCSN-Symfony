{# {% extends 'base.html.twig' %}

{% block title %}Profil
{% endblock %}

{% block body %}
	<h2>Bienvenue Dans Votre Profil Chère Chauffeur</h2>
	<div class="container-fluid profil  ">
		<div class="row  justify-content-center   ">
			<fieldset class="col-sm-12 col-md-3  ">
				<div class="cardh-100">
					<br>
					<strong class="strong">Bienvenue dans votre espace personnel,pour demander une cours appuyer sur  le lien ci-dessous</strong>
					<br>
					<br>
					<br>
					<br>
					<a href="{{path('chauffeur_newCours')}}" class="btn btn-warning btn-ld my-3 " style=" border-radius: 15px;width:100%;">Demmander une cours</a>
				</div>


			</fieldset>

		</div>

	</div>
{% endblock %} #}


{% extends 'base.html.twig' %}

{% block title %}Menue
{% endblock %}

{% block body %}


	<div class="container profil content ">
		<h1 class="titleContact text-center ">Menue<div class="clearfixTitle"></div>
		</h1>
		<div class="row  justify-content-center">

			<div class="col-md-4">
				<a class="btn submitMessage btnCustom w-100 mb-2" href="{{path('chauffeur_profil', {'id':app.user.id})}}">My profil</a>
				<a class="btn submitMessage btnCustom w-100 mb-2" href="{{path('passager_mesCommands', {'id':app.user.id})}}">My order</a>
				<a class="btn submitMessage btnCustom w-100 mb-2" href="{{path('passager_edit', {'id':app.user.id})}}">Modify my profil</a>
				{# <a class="btn submitMessage btnCustom" href="{{path('passager_suivreCommande', {'id':app.user.id})}}" >Follow my order</a> #}
				<a class="btn submitMessage btnCustom w-100 mb-2" href="{{path('rating_driver', {'id':app.user.id})}}">My review</a>
				<a href="{{path('chauffeur_newCours')}}" class="btn submitMessage btnCustom w-100 mb-2 ">Look for a client</a>
			</div>
		</div>
	</div>

{% endblock %}
